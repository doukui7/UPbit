name: "Coin Trade"

on:
  schedule:
    # 4시간마다 정각 실행 (UTC = KST-9)
    # KST 01:00/05:00/09:00/13:00/17:00/21:00
    - cron: "0 16,20,0,4,8,12 * * *"
  workflow_dispatch:
    inputs:
      run_job:
        description: Select job to run
        required: true
        type: choice
        default: trade
        options:
          - trade
          - manual_order
          - account_sync
      manual_order_params:
        description: '수동주문 JSON (예: {"coin":"BTC","side":"buy","pct":50})'
        required: false
        default: ""

permissions:
  contents: write

jobs:
  # ── 업비트 BTC 자동매매 (VM SSH 경유 - 고정 IP) ──
  trade:
    # schedule 제거: VM 스케줄러(minute==0)가 정시 실행 담당, GH Actions cron은 지연 발생하므로 수동 dispatch만
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_job == 'trade'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: master

      - name: Run Auto Trade via Cloud VM
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          command_timeout: 15m
          capture_stdout: true
          envs: UPBIT_ACCESS_KEY,UPBIT_SECRET_KEY,PORTFOLIO,TRADING_MODE,TELEGRAM_BOT_TOKEN,TELEGRAM_CHAT_ID
          script: |
            cd ~/upbit
            # 로컬 상태 파일 보존 (git reset 덮어쓰기 방지)
            cp signal_state.json /tmp/_signal_state_backup.json 2>/dev/null || true
            cp balance_cache.json /tmp/_balance_cache_backup.json 2>/dev/null || true
            git remote set-url origin https://github.com/doukui7/UPbit.git 2>/dev/null || true
            git fetch origin --quiet 2>/dev/null && git reset --hard origin/master --quiet 2>/dev/null || true
            cp /tmp/_signal_state_backup.json signal_state.json 2>/dev/null || true
            cp /tmp/_balance_cache_backup.json balance_cache.json 2>/dev/null || true
            bash scripts/vm_run_job.sh upbit
        env:
          UPBIT_ACCESS_KEY: ${{ secrets.UPBIT_ACCESS_KEY }}
          UPBIT_SECRET_KEY: ${{ secrets.UPBIT_SECRET_KEY }}
          PORTFOLIO: ${{ secrets.PORTFOLIO }}
          TRADING_MODE: upbit
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN || vars.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID || vars.TELEGRAM_CHAT_ID }}

      - name: Sync balance cache from VM
        env:
          VM_SSH_KEY: ${{ secrets.VM_SSH_KEY }}
          VM_HOST: ${{ secrets.VM_HOST }}
          VM_USER: ${{ secrets.VM_USER }}
        run: |
          mkdir -p ~/.ssh
          echo "${VM_SSH_KEY}" > ~/.ssh/vm_key
          chmod 600 ~/.ssh/vm_key
          ssh-keyscan -H "${VM_HOST}" >> ~/.ssh/known_hosts 2>/dev/null
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/balance_cache.json" ./balance_cache.json || echo "WARN: balance_cache.json not found"
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/signal_state.json" ./signal_state.json || echo "WARN: signal_state.json not found"
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/trade_log.json" ./trade_log.json || echo "WARN: trade_log.json not found"
          rm -f ~/.ssh/vm_key
          git config user.name "auto-trade-bot"
          git config user.email "bot@auto-trade"
          git add balance_cache.json signal_state.json trade_log.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "auto: trade 후 잔고/시그널 동기화"
            git pull --rebase origin master || true
            git push || echo "WARN: push failed, will retry next run"
          fi

  # ── 업비트 수동주문 (전략 분석 없이 즉시 매수/매도) ──
  manual_order:
    if: github.event.inputs.run_job == 'manual_order'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: master

      - name: Run Manual Order via Cloud VM
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          command_timeout: 5m
          capture_stdout: true
          envs: UPBIT_ACCESS_KEY,UPBIT_SECRET_KEY,TRADING_MODE,TELEGRAM_BOT_TOKEN,TELEGRAM_CHAT_ID,MANUAL_ORDER
          script: |
            cd ~/upbit
            cp signal_state.json /tmp/_signal_state_backup.json 2>/dev/null || true
            git remote set-url origin https://github.com/doukui7/UPbit.git 2>/dev/null || true
            git fetch origin --quiet 2>/dev/null && git reset --hard origin/master --quiet 2>/dev/null || true
            cp /tmp/_signal_state_backup.json signal_state.json 2>/dev/null || true
            bash scripts/vm_run_job.sh manual_order
        env:
          UPBIT_ACCESS_KEY: ${{ secrets.UPBIT_ACCESS_KEY }}
          UPBIT_SECRET_KEY: ${{ secrets.UPBIT_SECRET_KEY }}
          TRADING_MODE: manual_order
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN || vars.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID || vars.TELEGRAM_CHAT_ID }}
          MANUAL_ORDER: ${{ github.event.inputs.manual_order_params }}

      - name: Sync balance cache from VM
        env:
          VM_SSH_KEY: ${{ secrets.VM_SSH_KEY }}
          VM_HOST: ${{ secrets.VM_HOST }}
          VM_USER: ${{ secrets.VM_USER }}
        run: |
          mkdir -p ~/.ssh
          echo "${VM_SSH_KEY}" > ~/.ssh/vm_key
          chmod 600 ~/.ssh/vm_key
          ssh-keyscan -H "${VM_HOST}" >> ~/.ssh/known_hosts 2>/dev/null
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/balance_cache.json" ./balance_cache.json || echo "WARN: balance_cache.json not found"
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/signal_state.json" ./signal_state.json || echo "WARN: signal_state.json not found"
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/trade_log.json" ./trade_log.json || echo "WARN: trade_log.json not found"
          rm -f ~/.ssh/vm_key
          git config user.name "auto-trade-bot"
          git config user.email "bot@auto-trade"
          git add balance_cache.json signal_state.json trade_log.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "auto: 수동주문 후 잔고/시그널 동기화"
            git pull --rebase origin master || true
            git push || echo "WARN: push failed, will retry next run"
          fi

  # ── 계좌 데이터 VM 경유 동기화 (잔고/체결/입출금) ──
  account_sync:
    needs: trade
    if: always() && ((github.event_name == 'workflow_dispatch' && github.event.inputs.run_job == 'account_sync') || github.event_name == 'schedule')
    runs-on: ubuntu-latest
    steps:
      # 1) 리포 체크아웃 (push 용)
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: master

      # 2) VM에서 account_sync 실행 (GH_TOKEN 불필요)
      - name: Run Account Sync on VM
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          command_timeout: 5m
          envs: UPBIT_ACCESS_KEY,UPBIT_SECRET_KEY,TRADING_MODE,TELEGRAM_BOT_TOKEN,TELEGRAM_CHAT_ID
          script: |
            cd ~/upbit
            cp signal_state.json /tmp/_signal_state_backup.json 2>/dev/null || true
            git remote set-url origin https://github.com/doukui7/UPbit.git 2>/dev/null || true
            git fetch origin --quiet 2>/dev/null && git reset --hard origin/master --quiet 2>/dev/null || true
            cp /tmp/_signal_state_backup.json signal_state.json 2>/dev/null || true
            bash scripts/vm_run_job.sh account_sync
        env:
          UPBIT_ACCESS_KEY: ${{ secrets.UPBIT_ACCESS_KEY }}
          UPBIT_SECRET_KEY: ${{ secrets.UPBIT_SECRET_KEY }}
          TRADING_MODE: account_sync
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN || vars.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID || vars.TELEGRAM_CHAT_ID }}

      # 3) VM에서 캐시 파일 다운로드 → Runner에서 push
      - name: Download cache from VM and push
        env:
          VM_SSH_KEY: ${{ secrets.VM_SSH_KEY }}
          VM_HOST: ${{ secrets.VM_HOST }}
          VM_USER: ${{ secrets.VM_USER }}
        run: |
          # SSH 키 설정
          mkdir -p ~/.ssh
          echo "${VM_SSH_KEY}" > ~/.ssh/vm_key
          chmod 600 ~/.ssh/vm_key
          ssh-keyscan -H "${VM_HOST}" >> ~/.ssh/known_hosts 2>/dev/null

          # VM에서 캐시 파일 다운로드
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/account_cache.json" ./account_cache.json || echo "WARN: account_cache.json not found"
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/balance_cache.json" ./balance_cache.json || echo "WARN: balance_cache.json not found"
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/signal_state.json" ./signal_state.json || echo "WARN: signal_state.json not found"
          scp -i ~/.ssh/vm_key "${VM_USER}@${VM_HOST}:~/upbit/trade_log.json" ./trade_log.json || echo "WARN: trade_log.json not found"
          rm -f ~/.ssh/vm_key

          # 변경사항 있으면 commit & push (충돌 방지: pull --rebase)
          git config user.name "auto-trade-bot"
          git config user.email "bot@auto-trade"
          git add account_cache.json balance_cache.json signal_state.json trade_log.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "auto: 계좌 데이터 동기화"
            git pull --rebase origin master || true
            git push || echo "WARN: push failed, will retry next run"
          fi
